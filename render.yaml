services:
  - type: web
    name: pulse-chat-app
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        value: schat_persistent_jwt_secret_key_v1_2026
